import { Injectable } from '@angular/core';
import { environment } from '../../../environments/environment';

Injectable()
export class Globals{
    public static user = 'test@test.com';
    public static password = 'test12';
}